# Generación e historial técnico

Esta carpeta contiene material de trabajo. Los planos destinados a consulta de obra están en [`../planos_finales/`](../planos_finales/).

## Contenido

- [`flujo_svg_actual/`](flujo_svg_actual/) — sistema vigente, ligero y basado únicamente en SVG y JSON.
- [`historial/`](historial/) — revisiones, recursos anteriores, paquetes, vistas de control y PDFs históricos.

Para nuevos cambios use solamente `flujo_svg_actual/`. La estructura anterior se conserva para trazabilidad, no como punto de partida.

